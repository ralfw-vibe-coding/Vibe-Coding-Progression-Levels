{
  "tasks": {
    "serve": "deno run --allow-net --allow-read --allow-write --allow-env server/main.js",
    "migriere": "deno run --allow-read --allow-write server/migriere.js",
    "test": "deno test --allow-read --allow-write tests/",
    "coverage": "rm -rf coverage && deno test --allow-read --allow-write --coverage=coverage tests/ && deno coverage coverage",
    "hooks-einrichten": "git config core.hooksPath .githooks && echo 'pre-commit-Hook aktiv: die Testsuite läuft ab jetzt vor jedem Commit.'"
  }
}
